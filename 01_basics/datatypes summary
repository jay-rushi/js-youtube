// how to access data and how to store data
//  Primitive

//  7 types : String, Number, Boolearn, null, undefined, Symbol, BigInt

const score = 100
const scoreValue = 100.3

const isLoggedIn = false
const outsideTemp = null
let userEmail;

const id = Symbol('123')
const anotherId = Symbol('123')

console.log(id === anotherId);

// const bigNumber = 3456543576654356754n



// Reference (Non primitive) which can be allocated directly in memory 

// Array, Objects, Functions

const heros = ["shaktiman", "naagraj", "doga"]; //(the first line is stored in array [])
let myObj = {                                   // the second line is stored with variable and after that anything stored in {} is object 
    name: "hitesh",
    age: 22,
}

const myFunction = function(){                 // function is function(){}
    console.log("Hello world");
}

console.log(typeof anotherId);

// https://262.ecma-international.org/5.1/#sec-11.4.3

//**********************  imp  *****************
// *data type is added before the key operator and function is added after the key operator*  

let someNumber = 33

let stringNumber = String(someNumber)  

const gameName = new String('hitesh-hc-com')

